# Bug Report – JSON Alert Configuration

Tracking only confirmed defects on branch `specs/001-json-alert-config/design.md`.

# Closed Bugs
- **BUG‑001** – Active profile file tracked in git – *Closed 2025‑11‑17 via `fad1f80` (stored in `~/.config/...`).*
- **BUG‑002** – Win/loss duration comparison lost – *Closed 2025‑11‑17 via `fad1f80` (new context fields + preset updates).*
- **BUG‑003** – `validate` command changed active profile – *Closed 2025‑11‑17 via `fad1f80` (added `validate_profile`).*
